1.去掉ListView中的分割线

	android:divider="@null"

2.金钱中间设置横线

	((TextView) arg0.get(R.id.tv_real_money)).getPaint().setFlags(Paint.STRIKE_THRU_TEXT_FLAG);

3.控件获取焦点

        android:focusable="true"
        android:focusableInTouchMode="true"

4.